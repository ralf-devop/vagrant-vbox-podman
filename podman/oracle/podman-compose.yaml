
services:

  db:
    image: container-registry.oracle.com/database/express:latest
    restart: always
    environment:
      ORACLE_PWD: example
    volumes:
      - /var/data/oracle:/opt/oracle/oradata
    ports:
      - 1521:1521
